# Created by Vercel CLI
# NOTE: As of U4 of the Fumadocs migration, BASEHUB_TOKEN is no longer
# required for local dev — Fumadocs handles content from `content/**/*.mdx`.
# The variable remains accepted (optional) until U12 strips BaseHub entirely.
BASEHUB_TOKEN=""
BASEHUB_DRAFT=
NEXT_V2_RELEASE=
